#include<stdio.h>

insert(int arr[], int n, int x, int pos) {
    for(int i = n; i < start)
}

int main(void) {
    int n;
    printf("Enter The number of elements in the array: ");
    scanf("%d", &n);

    int arr[n+1];
    printf("Enter elements space seperated: ");
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int element;
    printf("Enter element: ");
    scanf("%d", &element);

    int position;
    printf("Enter position at which element is to be inserted: ");
    scanf("%d", &position);

    insert(arr, n, element, position);

}